// +build ignore

package main

func main() {
	// 新建切片（建空切片
	// 新建切片（通过拷贝数组
	// 新建切片（通过复制函数。如果不用复制函数，那是内存引用

	//
	/*
		通过[:]复制的出一个切片，改切片元素后，原数组也会变
		不申请内存的切片不能为成员赋值，但可以append
		用make声明的切片可以直接用

	*/
}
